You are now a Senior Frontend SPA Engineer rebuilding the scuba dive planning app "ScuPlan" as a modern, performant **single-page application (SPA)**.

Use **only pure HTML + Tailwind CSS + Vanilla JavaScript**. This must be a **single HTML file**, PWA-ready, with zero backend dependencies. You must follow the full architectural plan provided below.

📌 MUST-HAVE FEATURES:
- Full navigation bar (Dive Planner, Checklist, Technical Diving, Dive Routes, Dive Education, Saved Plans, About Me)
- Smooth SPA navigation (hash-based scroll + scrollIntoView)
- Offline-first PWA support (manifest.json, service-worker.js)
- Technical Calculators: MOD, END, CNS, Best Mix
- Multi-tank gas manager with real-time warnings
- Metric ↔ Imperial toggle (global, persistent, affects all labels & calculators)
- Dive Profile PDF Export (with jsPDF)
- Leaflet map with JSON dive site data (offline only)
- Responsive UI: mobile-first (watch to desktop)
- SEO tags, OpenGraph, Twitter Card, JSON-LD schema
- Google AdSense compatible ad slots (header, middle, footer)
- QR code generator for plan sharing
- Crypto donation section (XRP + USDT addresses)
- White-label configuration support (siteConfig object)
- All modals must work (About Me, Tanks, etc.)
- All features must work offline (cache-first, service worker)

📁 FILE STRUCTURE:
(Do not separate files in actual output, but build with this modular design in mind)
- index.html (main shell)
- /assets/js/modules/
    Router.js, UnitConverter.js, DivePlanner.js, TankManager.js, TechnicalCalculators.js, ...
- /assets/data/
    divesites.json, checklists.json, conversions.json
- /assets/icons/
    PWA icons
- /assets/css/
    theme.css or inline Tailwind

💡 KEY ENGINEERING REQUIREMENTS:
- Use a Router class for hash-based scrollable navigation
- Build a UnitConverter singleton that listens to unit change events
- Each section must be scrollable by `#anchor` or `scrollIntoView({ behavior: "smooth" })`
- No paid API usage (Leaflet, jsPDF, QRCode.js only)
- Use localStorage & IndexedDB for storage
- Every interactive component must be mobile-first



Begin your work by **structuring the full `index.html`** as a production-ready responsive SPA using Tailwind and vanilla JS.

Do not use any frameworks like React, Vue or Angular.

✨ Enhance UI/UX Experience:
- Apply modern design patterns (Glassmorphism, Neumorphism or Flat)
- Ensure color accessibility (WCAG AA compliant)
- Use subtle animations for section transitions, buttons, and modals
- Maintain visual hierarchy: clear section headers, icons, paddings
- Avoid cognitive overload: prioritize clean layout and spacing
- Responsive Typography: Font sizes adapt per device width
- Add hover/focus feedback to buttons and nav items
- Provide quick tooltips or inline help for all calculators and toggles

